function demo(x1,y1,x2,y2){
    let first = function(){
        return Math.sqrt(Math.pow(x2-x1)+ Math.pow(y2-y1))
    }
    let second = function(){
        return Math.sqrt(Math.pow(x2-x1)+ Math.pow(y2-y1))
    }
    let third = function(){
        return Math.sqrt(Math.pow(x2-x1)+ Math.pow(y2-y1))
    }
}

let dice = function(){
    return Math.sqrt(number);
}